import React, {
  Fragment
} from 'react'
import './index.css'

const GridDecorationLines = () => {
  return ( <
    Fragment > { /* <div className="line decor-line" /> */ } <
    div className = "line decor-line line--right" / >
    <
    div className = "line decor-line line--left" / >
    <
    /Fragment>
  )
}

export default GridDecorationLines